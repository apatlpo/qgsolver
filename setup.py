#!/usr/bin/env python

from distutils.core import setup

#INSTALL_REQUIRES = ['xarray >= 0.10.6', ]

setup(name='qgsolver',
      description='Quasigeostrophic solver',
      url='https://github.com/apatlpo/qgsolver',
      packages=['qgsolver'])

#      install_requires=INSTALL_REQUIRES,


